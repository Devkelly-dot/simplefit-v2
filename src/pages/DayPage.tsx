interface Props {
    title: string;
    description: string;
}
const DayPage: React.FC<Props> = ({ title, description }) => {
    return (
        <div>

        </div>
    );
}

export default DayPage;